import { styled } from "@linaria/react";

export const Title = styled.h1`
  font-size: 2em;
  margin-top: 1em;
  margin-bottom: 1em;
`;

export type Colors = "primary" | "secondary" | "success";

export const theme = {
  color: "red",
};
